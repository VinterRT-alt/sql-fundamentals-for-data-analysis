# 🧪 Ejercicios de SQL: Filtrado Básico con WHERE

Este directorio contiene una serie de ejercicios prácticos para aprender y practicar el filtrado de datos en SQL utilizando la cláusula `WHERE`. Los ejercicios están basados en la base de datos de ejemplo **classicmodels**, una base de datos clásica para aprender SQL.

## 📌 Objetivos

- Familiarizarse con la sintaxis básica de `SELECT` y `WHERE`.
- Aprender a usar operadores de comparación (`=`, `>`, `<`, etc.).
- Utilizar el operador `IN` para filtrar por una lista de valores.
- Combinar condiciones con `AND`.
- Filtrar rangos con `BETWEEN` y `NOT BETWEEN`.
- Usar `LIKE` para búsquedas con patrones (comodines).

## 🗂️ Estructura de los archivos

Los ejercicios están organizados en un único archivo SQL (`02_where_filtros.sql`) con cada consulta comentada y numerada. También se incluye este README para documentación.
